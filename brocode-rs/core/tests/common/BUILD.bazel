load("//:defs.bzl", "brocode_rust_crate")

brocode_rust_crate(
    name = "common",
    crate_name = "core_test_support",
    crate_srcs = glob(["*.rs"]),
    lib_data_extra = [
        "//brocode-rs/core:model_availability_nux_fixtures",
    ],
)
